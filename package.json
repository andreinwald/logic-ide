{
  "name": "code-viewer",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:main && npm run build:renderer && npm run copy:static",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "esbuild src/renderer.ts --bundle --platform=browser --format=esm --outfile=dist/renderer.js",
    "copy:static": "mkdir -p dist/assets && cp src/index.html dist/index.html && cp src/styles.css dist/styles.css && cp src/assets/seti.woff dist/assets/seti.woff",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "check": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andreinwald/code-viewer.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/andreinwald/code-viewer/issues"
  },
  "homepage": "https://github.com/andreinwald/code-viewer#readme",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "electron": "^41.0.4",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.92",
    "ignore": "^7.0.5",
    "marked": "^17.0.5"
  }
}
